[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "oura-data-saver"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "pg8000>=1.31",
]

[project.scripts]
oura-data-saver = "oura_data_saver.main:main"
